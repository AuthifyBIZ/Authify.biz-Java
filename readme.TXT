necessary libraries to use the class and the example :
bcprov (bouncy castle provider)
java curl
org json

there's no public key pinning in this class,
that shouldn't be a problem unless someone gets your api key, 
in that case it is recommended to change it as fast as possible 